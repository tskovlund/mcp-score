name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

jobs:
  analyze:
    uses: tskovlund/.github/.github/workflows/codeql.yml@5bb0d20b76da99a9a20c0beb03229d2a56d83fb0 # main
    with:
      language: python
    permissions:
      security-events: write
